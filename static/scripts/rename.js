const listOfRingtones = document.querySelector("#list-of-ringtones")

function rename(e) {
    if (e.target.tagName === "IMG" && e.target.parentElement.classList.contains("name")) {
        
    }
}

listOfRingtones.addEventListener("click", rename)